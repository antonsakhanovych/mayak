# Copy to mayak-mc/.env and edit. Gitignored.
#
# Uses itzg/minecraft-server's real variable name directly (MEMORY) - this
# file is loaded via docker-compose.yml's env_file, which injects values
# straight into the container with no renaming/default layer.
MEMORY=4G
