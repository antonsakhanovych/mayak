# Git-tracked: fixed backup policy, same across every deployment (not
# host-specific, unlike the secrets in mayak-backup/.env).
BACKUP_INTERVAL=3600         # seconds between backup cycles
BACKUP_STARTUP_DELAY=60      # grace period before the first cycle
RETENTION_HOURLY=24
RETENTION_DAILY=7
RETENTION_WEEKLY=4
